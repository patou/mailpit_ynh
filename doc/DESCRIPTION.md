Mailpit is a modern email testing tool for developers and administrators.

It provides:

- an SMTP server to capture outgoing emails from your apps,
- a web interface to inspect messages, headers and attachments,
- a local message database with persistence across restarts.
